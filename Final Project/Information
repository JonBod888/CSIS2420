
I have not gotten around to giving functionality to the save button, so no CSV file. 

Sad.png looks more like mad, just in case that confuses you

There are 25 images in total

There are 21 classes in total (though some of them could probably be separated further)

Anything that goes into the paint() or playGame() methods in the GameFrame Class will be run about 10 times per second, so keep that in mind if want to experiment a bit
